{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for dua-ruqyah frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "lint": "echo 'No lint configured for backend'"
  },
  "author": "Mehrab Chowdhury",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.18",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/cors": "^2.8.18",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "sqlite3": "^5.1.7",
    "ts-node-dev": "^2.0.0"
  }
}